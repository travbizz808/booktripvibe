plugins {
    id 'com.android.application'
}

android {
    namespace 'com.booktripvibe.crm'
    compileSdk 35

    defaultConfig {
        applicationId 'com.booktripvibe.crm'
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildFeatures {
        buildConfig true
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
